This full stack application will attempt to create: a functional back end running on Postgres, served via Express and Knex, and displayed with a UI created in React with Vite. 
The creation of the back end will be RESTful and suitable to support CRUD functions through routes on the React component.
The front end will be basic, but will neatly display the fetched data and have a separate routes for updating and creating new API entries.
